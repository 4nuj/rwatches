  <div class="card-body">
    <div class="container flex-column">
      <div class="img-wrap">
        <% if watch.image.attached? %>
          <%= image_tag watch.image, class: "card-image img-fluid rounded-circle" %>
        <% end %>
      </div>
      <div class="d-flex justify-content-start">
        <h2><%= watch.brand %>-<%= watch.model %></h2>
      </div>
      <div class="d-flex justify-content-start">
        <h3>£<%= watch.price %></h3>
      </div>
    </div>
  </div>
